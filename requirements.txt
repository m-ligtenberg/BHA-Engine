# Core GUI Framework
PyQt6>=6.5.0

# MIDI handling (lightweight)
mido>=1.3.0
python-rtmidi>=1.5.0

# Audio processing (minimal overhead)
sounddevice>=0.4.6
numpy>=1.24.0

# Configuration management
PyYAML>=6.0

# Optional dependencies (load when needed)
scipy>=1.10.0          # Advanced audio processing
librosa>=0.10.0        # Sample analysis

# Development dependencies (separate install)
pytest>=7.0.0          # Testing framework
black>=22.0.0          # Code formatting
pylint>=2.15.0         # Code linting